# Step out debugger

Step out of the current frame in the debugger with a given ID, running until the current frame returns

**URL**: `/debuggers/{id}/frames/{index}/stepout`

**Method**: `POST`

## Success Responses

**Code** : `200 OK`
